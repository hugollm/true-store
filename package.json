{
    "name": "true-store",
    "version": "0.4.1",
    "description": "Dead simple state management for javascript applications.",
    "license": "MIT",
    "author": {
        "name": "Hugo Mota",
        "url": "https://github.com/hugollm"
    },
    "repository": {
        "type": "git",
        "url": "git://github.com/hugollm/true-store.git"
    },
    "main": "src/store.js",
    "scripts": {
        "test": "jest --coverage",
        "watch-tests": "jest --coverage --watchAll",
        "coveralls": "cat coverage/lcov.info | coveralls",
        "list-package-files": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz"
    },
    "dependencies": {
        "immutable": "3.8.2"
    },
    "devDependencies": {
        "coveralls": "*",
        "jest": "*"
    },
    "jest": {
        "testEnvironment": "node",
        "verbose": false
    }
}
